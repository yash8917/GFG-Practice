class Compute
{
    String average(int A[], int N)
    {
        float sum=0;
        for(int i=0 ; i<N ; i++){
            sum+=A[i];
        }
         float avg = sum/N;
         String sN=String.format("%.2f",avg);
      
       return sN;
    }
}
